require.config({
    paths: {
        Factory: "./factory",
        app: "./app",
        Item: "./models/Item",
        Container: "./models/Container",
        Section: "./models/Section"
    }
});

require(['app'], function(app) {

});